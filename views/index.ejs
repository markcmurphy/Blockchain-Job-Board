<!DOCTYPE html>
<html>

<head>
    <% include ./partials/head.ejs %>
</head>

<body>
<header>


  <nav class="navbar navbar-default">
    <div class="container-fluid">
      <div class="navbar-header">
    <a class="navbar-brand" href="/">Blockchain Jobs</a>
  </div>
  <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">

      <ul class="navbar-nav">
        <li class="nav-item"><a class="nav-link" href="/employers">Employers</li>
    <li class="nav-item"><a class="nav-link" href="/jobs">Jobs</li>
      <% if(currentUser) { %>
    <li class="nav-item"><a class="nav-link" href="/sessions/logout">Log Out</a></li>
        <% } %>
          <% if(!currentUser) { %>
            <li class="nav-item"><a class="nav-link" href="/sessions/login">Log In</a></li>
            <% } %>
              <% if(!currentUser) { %>
                <li class="nav-item"><a class="nav-link" href="/sessions/register">Register</a></li>
                <% } %>
      </ul>

    </div>
  </nav>
</header>

<h1>
    <% if(currentUser) { %>
        Welcome <%=currentUser.username%>!
    <% } else { %>
        Welcome!
    <% } %>
</h1>
</body>

</html>
